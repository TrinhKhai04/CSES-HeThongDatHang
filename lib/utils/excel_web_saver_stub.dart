// utils/excel_web_saver_stub.dart

Future<String> saveExcelFile(
    List<int> bytes, {
      required String baseName,
    }) async {
  throw UnsupportedError(
    'saveExcelFile không được hỗ trợ trên nền tảng này',
  );
}
